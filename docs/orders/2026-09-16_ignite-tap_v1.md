# UNDERBORN 実装指示：点火タップ便 v1 — 拍に合わせて叩く点火（石皿の拍、新旧切替）

> 設計：Claude（2026-09-16）。`docs/design/ignition-tap-design.md`（デザインスレの持ち込み `docs/design/ignition-tap-handoff.md` への答え）。Kevin「つづけて」で着手。**これは試作**：開発フラグで新旧を切り替え、Kevin が1周遊んでから確定する。灯籠の拍と絵は入れない（石皿の拍だけ。絵は今のまま）。
> 流れ：この発注文 → レビュー用 Sol → Claude が直す → 実装用 Sol → Claude 検品（点火は遊びの核なのでレビューあり）

## 0. この指示書について
- 対象：`game/underborn_scout8m.html`（公開スイッチ便、`fc64038`）→ **出力 `game/underborn_scout8n.html`**
- 行番号は 8m のもの：点火判定 L1817〜、`igniteNeed` L494、`applyCoreIgniteCost` L505、輪のゲージ L3025、状態の文 L4112、pointerdown L4412、keydown L4335
- Kevin 待ちなし。数値は暫定（DECISIONS に暫定行）

### 壊すな指定
- 旧モード（`P.igniteMode==='wait'`）の挙動は今のまま残す（切替で完全に戻る）
- 種火配下の連鎖点火（操作なし）、逃げる祭壇、暗火・息吹の例外、`CANON_POEMS`、祭壇の絵（見た目の段階で）

---

## 1. 決定（設計の答え、Kevin 未確定＝試作）
| 項目 | 新モード（`tap`） |
| --- | --- |
| 合図 | 祭壇の隣に立ち止まると（今の `near&&master.t>=1&&canIgniteNow()`）、祭壇の冷たい火が**一定の拍で脈打つ**。石皿の拍＝**0.28秒**（`P.tapBeat`） |
| 叩く | 脈の山の前後 **±0.09秒**（`P.tapWindow`）の間に **タップ／クリック（画面のどこでも）または Enter**。E はハーケン、スペースは息止めなので使わない |
| 当たり | 火花が1つ残り、種火が育つ（`tapHits++`）。必要な当たり数 `tapNeed()`＝段階1：**4**、段階2：3、段階3：2、真の姿：**1**（`igniteNeed` の置き換え）。暗火コアは**切り上げで半分**（4→2、3→2、2→1、1→1） |
| 外れ | 拍の外で叩く／山を1回飛ばす → 鈍い音（`tone(180,.08,'square',.05)`）と火花が1つ消える（`tapHits=max(0,tapHits-1)`）。**全部は消えない** |
| 点火 | `tapHits>=tapNeed()` で今の点火処理（L1817 の中身：`near.mine=true` 以降）をそのまま呼ぶ |
| 中断 | 離れる／住民に触れる／息止めを始める → `tapHits=0`（今の `takeT` リセットと同じ条件） |
| 見せ方 | **輪のゲージ（L3025）は新モードでは描かない**。代わりに祭壇の上に「火花の粒」が `tapHits` 個、脈に合わせて小さく揺れる。脈の山で祭壇の火が一瞬明るく（合図） |
| 音 | 脈の山で小さな「チッ」（`tone(1400,.03,'sine',.025)`、合図）。当たり「カッ」（`tone(900,.05,'square',.06)`＋火花）。外れ「ボス」（上） |
| 状態の文 | L4112「祭壇に火を移している」→ 新モードでは「拍に合わせて 火を打つ」（暫定） |
| 切替 | `P.igniteMode='tap'|'wait'`。**既定は `tap`**（Kevin が試すため）。開発パネルに「点火：拍／待ち」と「拍の速さ 0.18〜0.5」「必要数 2〜6」 |

## 2. 実装ステップ
### チャンク1
#### Step 1: 状態と判定
- `P.igniteMode='tap', P.tapBeat=0.28, P.tapWindow=0.09`。`tapT`（拍の時計）、`tapHits`、`tapTarget`（隣の祭壇）
- L1817 の分岐を `P.igniteMode==='wait'` なら今どおり、`'tap'` なら：隣接中は `tapT+=dt`、山＝`tapT % tapBeat` が 0 をまたぐ瞬間。叩き入力（pointerdown／Enter keydown。**祭壇の隣接中だけ**、他の用途の入力より先に処理し、消費する）が山の ±window なら当たり、外なら外れ。山を window 以上過ぎても叩かなければ「飛ばし」で外れ
- 点火後は `tapHits=0`、次の祭壇でまた0から
#### Step 2: 見せ方と音（§1）
#### Step 3: 開発パネルと状態の文
- 完了条件：1F の祭壇の隣で、脈に合わせて Enter を4回当てると点火。外すと粒が1つ減る。離れると0。`wait` に切り替えると今の1.4秒待ちに戻る。種火配下の連鎖は操作なしで今どおり

**ここでチャンク1完了報告。**

## 3. 完了条件（便全体）
1. 新規開始 → 1F で拍の点火ができる（文字の説明なしで合図が分かるか、Sol の感触も1行）
2. 段階2・3・真の姿・暗火で必要数が変わる（開発パネルの段階固定で確認）
3. `wait` で旧挙動に完全に戻る
4. 逃げる祭壇：動いている間は叩けない（止まっている4秒の間だけ拍が出る）
5. 正典の詩・絵は不変
6. Sol：1〜4 を画面で確認して HANDOFF に書く。スマホ幅（390px）でタップが効くことも

## 4. スコープ外
- 灯籠（2種目の拍）、祭壇の絵の差し替え、輪のゲージの完全削除（旧モードでは残す）

## 5. 未決事項
- 暫定（Kevin が1周してから）：拍 0.28 秒、必要数 4、窓 ±0.09、外れで1つ減る、既定を tap にする
- Kevin の最終判断：この方式を採るか（採らなければ `wait` に戻して終わり）

## 6. 出力形式
- 差分で出力。チャンク完了時に報告。反映／見送り／質問の3区分。食い違いは報告

## 7. 正典の更新（完了後）
- `GAME_DESIGN.md` 光と息止め／点火の節に「拍に合わせて叩く（試作、開発フラグ）」
- `docs/orders/README.md`、`HANDOFF.md`、`DECISIONS.md`（暫定行）
