# UNDERBORN 実装指示：公開スイッチ便 v1 — DEV_DEFAULT=false、URL で開発パネルを出す

> 設計：Claude（2026-09-15）。遊び側の便が終わったので、本番の既定を「開発パネルなし」にする。開発は続くので、URL の合図で今までどおり出せるようにする。**レビュー用 Sol は飛ばす。**

## 0. この指示書について
- 対象：`game/underborn_scout8l.html`（台詞便、`971e7f9`）→ **出力 `game/underborn_scout8m.html`**
- Kevin 待ちなし

### 壊すな指定
- ゲームの中身すべて。変えるのは開発パネルの既定と出し方だけ

## 1. 直す
- `DEV_DEFAULT=false`（L141）。**`?dev=1`／`?dev=0` と「デバッグ保存」は既に実装済み（L148）なので、そのまま**
- 開発パネルが無効のとき：右上の「数値」ボタン、左下の「DEV」印、HUD の詳細行（配下・住民の数など）を出さない。ハート・粒・階・本数だけ
- タイトルに小さく版の番号（`v0.8m` のように、ファイル名の末尾）を右下に薄く。公開の問い合わせ用
- 完了条件：素の URL で開くと開発パネルも DEV 印も無い。`?dev=1` で今までどおり。Sol は両方をスクリーンショット

## 2. 正典の更新（完了後）
- `README.md`（遊び方に `?dev=1` を1行）、`docs/orders/README.md`、`HANDOFF.md`、`GAME_DESIGN.md` 技術方針
