# UNDERBORN 実装指示：ライター便 hotfix v1 — 保留（連鎖ポーズ・送り先選択）のあとの1打を必ず当たりに

> 設計：Claude（2026-09-18）。8o 検品で見つけた1点。原因は発注文（ライター便 v2）の穴：保留中に「冷め時計」は止めたが「打の間隔の測定」は止めていないため、連鎖ポーズ（1.5秒）を待ってから叩くと間隔が前の 1.8 倍を超えて「乱れた」になる。Kevin の感想②「配下が他の祭壇を灯すと続きが分からなくなる」の場面そのもの。レビュー用 Sol は飛ばす（差分1行＋確認）。

## 0. 対象
- `game/underborn_scout8o.html`（`8a19f17`）→ **出力 `game/underborn_scout8p.html`**、`BUILD_VERSION='v0.8p'`
- 壊すな指定：ライター便 v2 の他の挙動すべて、`wait` モード、`CANON_POEMS`

## 1. 変更（1点）
- `updateTapIgnition` の保留分岐 `if(sparkSelect||chainPause>0){ if(tapCoolAt!==null) tapCoolAt+=rawDt; return; }` に **`tapPrevIv=null;`** を足す。
  - 意味：保留に入ったら比較間隔を捨てる → 保留中に叩いても、保留が明けてから叩いても、**その1打は必ず当たり**（冷めたあとの最初の打と同じ扱い）。その次の打から再び間隔を比べる。`tapHits`・`tapLastT`・冷め時計の保持は今のまま。
  - Claude がページ上で同じ置き換えを当てて確認済み：2打→連鎖ポーズ 1.5 秒を待つ→3打目で点く／2打→連鎖ポーズ中に3打目で点く／送り先選択中は叩けず燠は残る→選択後の1打で点く。

## 2. 完了条件
1. 1F で2打のあと、開発操作で `chainPause=1.5` を入れて 1.8 秒待ってから Enter → 点く（8o では燠が1つ減って点かない）
2. 同じく連鎖ポーズ中に Enter → 点く
3. 空振り 0.22 秒・乱れ・冷め・段階別 3/2/2/1・`wait` は 8o のまま（回帰なし）
4. `CANON_POEMS` 不変。`HANDOFF.md` に確認を1〜3行

## 3. 出力形式
- 差分で出力。反映／見送り／質問の3区分
