# UNDERBORN 実装指示：台詞便 v1 — 最終稿の反映、初進化の音、休憩所の主の目印、プレビューの店

> 設計：Claude（2026-09-15）。Kevin の台詞の最終決定（9-15、`docs/design/lines-changes.md` #1〜#10）。**レビュー用 Sol は飛ばす。** これで遊び側の便は終わり。次は `DEV_DEFAULT=false` と見た目。

## 0. この指示書について
- 対象：`game/underborn_scout8k.html`（店の流れ便、`75599be`）→ **出力 `game/underborn_scout8l.html`**
- 行番号は 8k のもの。Kevin 待ちなし

### 壊すな指定
- `CANON_POEMS` の既存の詩（opening/child/lord/release/trueForm/land）は**本文・改行とも不変**。変えるのは `callback.child` の削除と、`thanksYes` の追加だけ
- 住民・店・帯の文はすべて**最終稿として確定**（変更しない）

## 1. 直す
### Fix 1: 「はい」のあとのお礼（#1）
- `CANON_POEMS.thanksYes=['ありがとう、小さき火。']` を足す。`chooseGoddessAnswer`（L3556 付近）で「はい」のとき、`startGoddessTalk` の**最初の行の前に** `thanksYes[0]` を挟む（文字送り → ▼ → 次で「さぁ、起き上がって……」）
- 完了条件：はい → 「ありがとう、小さき火。」→ 開始詩2行目

### Fix 2: 初進化の回想を削除（#2）
- L1222 の `callback.child` の挿入をやめる。`CANON_POEMS.callback.child` は**削除**（`release` は残す）
- 完了条件：3回以上断った保存でも、初進化に回想が出ない。エンディングの回想は出る

### Fix 3: 初進化にベイビーの喜ぶ音（#3）
- `startEvolution`（L1199）の変身の瞬間（姿が変わるフレーム）に、短い上がる3音：`tone(784,.07,'triangle',.06)`→`tone(988,.07,'triangle',.06,.08)`→`tone(1319,.18,'triangle',.07,.16)`。2回目の進化（ロード）も同じ
- 完了条件：進化の変身で3音が鳴る

### Fix 4: 休憩所の主の目印（#6）
- 主（`e.host`）の頭上に「✦」（HUD の粒と同じ記号・色）を浮かせて脈打たせる（上下に小さく、1.2秒周期）
- 主の足元に**敷物**（暗い赤の長方形）を描き、その上に品5つを並べる（水＝瓶、絵＝小さな額3つ、鳥＝小さな鳥。今の仮の絵を少し大きく）
- **「▶ はなす」**：休憩所で、話しかけられる相手（主・座っている住民・記録の石）に隣接しているとき、その頭上に小さく「▶ はなす」を出す（白、`CS*.02` 程度）。会話中は出さない
- 完了条件：主が一目で分かる。隣に立つと「▶ はなす」

### Fix 5: 開発パネル「休憩所を試す」で店が開く（#7）
- プレビュー中は `roomOverride`（部屋便②の型）に**仮の部屋**（`sparks:20`、他は今の `campaign.room` の写し）を入れて店を開けるようにする。買い物は仮の部屋に反映、`campaign.room` は書き換えない。プレビューを出たら解除
- 完了条件：数値パネルの「休憩所を試す」で主に話しかけて水を買える。本番の粒は変わらない

### Fix 6: エンディングの浄化で「長い夜が明けた。」（#10、Kevin 9-15）
- 地上の浄化（`purify`）が 1.0 に達した瞬間（地上に出て約4秒、歩き出す直前）に、画面中央やや上に「**長い夜が明けた。**」を静かに浮かべる（「火が絶えた」と同じ描き方、白、1.8秒で消える）。**飾った絵に関係なく出す**（太陽なら日の出と重なる。月・星空・無しでも「ダンジョンの夜が明けた」の意味で出す。暫定）
- 「旅の果て」の文は今のまま（「小さき火は、地上へたどり着いた。」）
- 完了条件：浄化が終わった瞬間に1行、歩き出す前に消える

## 2. 完了条件（便全体）
1. 新規開始 → はい → お礼の1行。3回断った保存 → 初進化に回想なし、エンディングに回想あり
2. 進化で3音。休憩所で主の ✦ と「▶ はなす」。プレビューで買える
3. 浄化の終わりに「長い夜が明けた。」
4. 既存の詩と住民・店・帯の文は不変
5. Sol：1〜3 を画面で確認して HANDOFF に書く

## 3. 正典の更新（完了後）
- `GAME_DESIGN.md`：詩の表に `thanksYes` を追加、`callback.child` を削除、休憩所の節に主の目印、演出欄に初進化の音
- `docs/orders/README.md`、`HANDOFF.md`、`DECISIONS.md`（台詞の最終稿＝確定）、`docs/design/lines-2026-09-15.md` を最終版に
