# AI共同作業ルール

このリポジトリは、アンダーボーン制作の正典です。チャット履歴より、GitHub上の最新版を優先してください。

## モデルの分担（2026-09-05）

- 実装・コミット・発注文とコードの照合：**GPT-5.6 Sol**
- 便ごとの起動確認・完了条件のチェック（ブラウザで動かして見る）：**Sol**（実装した本人でよい）
- 長い通しプレイと手触りの判定（50階の所要時間、理不尽さ、逃げ切れすぎ）：**GPT-6 Astra**。実装には使わない。1便ごとに新しいスレッドで、それだけを頼む（9-06 Kevin 確認）
- 設計・発注文・差分の検品・詩の草稿・台帳の更新：Claude
- **発注文レビュー（実装の前）：Sol の別スレッド（レビュー専用、実装スレッドとは分ける）**。発注文とコードを照合し、関数名・行番号の誤り、壊すな指定の抜け、正典（GAME_DESIGN／DECISIONS／REJECTED_IDEAS）との食い違いを指摘する。**コードは書かない、直さない。** 指摘は「発注文の何行目／何が／どう直すか」の形で。実装スレッドはレビュー済みの発注文だけを受け取る（9-06 Kevin）

## 作業開始時

1. `README.md`、`GAME_DESIGN.md`、`DECISIONS.md`、`REJECTED_IDEAS.md`、`TASKS.md`、`HANDOFF.md`を読む。発注文を扱う場合は `docs/orders/README.md` も読む。
2. 依頼内容と既存仕様の矛盾を確認する。
3. いきなり変更せず、何をどうするか、対象ファイル、見送る範囲を先に説明する。
4. 不明点があっても安全に進められる調査は行い、結果が大きく変わる質問だけをユーザーへ確認する。

## 作業中

- ユーザーが指定した段階と範囲を守る。
- 依頼されていない後工程や機能を先回りして実装しない。**例外（2026-09-03）**：`TASKS.md`「進行中」に並ぶ便（7B-b → 7C → DEV 戻し）は、1便を終えて HANDOFF を更新したら、ユーザーを待たずに次の便へ進んでよい。次の便の発注文が `docs/orders/` に無ければそこで止まる。
- **止まる条件（2026-09-03）**：`DECISIONS.md` 2026-09-03 の「最終決定4つ」に触れる判断が要るとき／`REJECTED_IDEAS.md` の却下案と衝突するとき／確認・テストが通らないとき。それ以外の判断は `DECISIONS.md` に「暫定」行を書いて進める（ユーザーは却下だけする。黙認＝採用）。
- **プレイ検品（2026-09-03）**：ブラウザでゲームを実際に動かして測る・確かめる作業は Codex の担当。所要時間は数字で、手触りは「不自然だった場面」を HANDOFF「実装側の感触」に書く。
- 既存の決定や実装を、理由なく削除・上書きしない。
- 同時作業を避ける。当面は一人のAIが反映を完了してから次のAIが着手する。
- 秘密情報、認証情報、個人情報をコミットしない。

## 作業終了時

1. 必要に応じて `GAME_DESIGN.md` と `DECISIONS.md` を更新する。
2. `TASKS.md` の状態を更新する。
3. `HANDOFF.md` に変更内容、確認結果、残件、次の一手を書く。
4. 実装を行った場合は、関連する確認・テスト結果を記録する。
5. GitHubへ書き込める担当は、変更対象だけをコミットして反映する。
6. 書き込めない担当は、完成ファイルまたは適用可能なdiff、確認結果、引き継ぎを提出し、コミット済みとは報告しない。反映は書き込み可能な担当へ引き継ぐ。

## 完了報告

- 反映したもの
- 見送ったもの
- 確認・テスト結果
- 残っている質問や次の作業

を短く明確に示してください。
- **検品の必須項目（2026-09-08 追加）**：コードの筋を追うだけでなく、**新規開始（`localStorage` 空）の経路を画面で見る**。Kevin が 7g で3件（床のハーケン先出し／カーソルが動かない／穴が暗闇に隠れる）を初回プレイで見つけた。いずれも開発ボタン経由の確認と既読フラグ付きの確認では出ない。Sol の HANDOFF「確認」欄と Claude の検品の両方で、新規開始からの最初の数分をスクリーンショットで見る。
