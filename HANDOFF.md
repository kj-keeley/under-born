# 引き継ぎ

## 現在の状況

- GitHub: `kj-keeley/under-born` / 既定ブランチ `main` / **公開**
- **ゲームは実装済みで遊べる**。正典は `game/underborn_scout6.html`（2932行・HTML単一ファイル）
- 第6便まで納品・検品済み。第7便は7A・7B・7Cに分割し、**7Aの発注文がレビュー通過済み・発射待ち**
- 開発体制: 設計＝Claude／実装＝GPT Codex／レビュー＝GPT／検品＝Codexセルフレビュー＋Claude＋Kevinプレイ

## 今回行ったこと（2026-08-08）

1. 第7便の発注文を作成（v1）→ GPTレビューで6点指摘 → v2で反映
2. 設計方針が大きく動いたため、第7便を**7A・7B・7C**へ分割
   - 全土地の階数を大幅拡張（遺跡50／砂漠42／火山44／沼38／森39）
   - 失敗と復帰の仕組みとして**支点（プレイヤーが置くチェックポイント）**を新設
3. 7Aの発注文を作成（v1）→ GPTレビューで3点指摘 → v2で反映（**GO判定取得済み**）
4. 各土地の詩4篇を確定。語り手を女神と土地の二声に分離
5. 既存の設計内容をこのリポジトリの資料へ移行
6. Codexが提出物11ファイルを検査してリポジトリへ反映
   - `game/underborn_scout6.html` を正典パスとして採用（内容は無変更）
   - READMEの「未実装」を現況に合わせて修正
   - 書き込み権限のないAIは完成ファイルまたはdiffを渡す運用へ規約を修正

## 確認・検証結果

- 発注文に書いたシンボル名・行番号は `game/underborn_scout6.html` に対して検索で裏を取った
- レビューで検出された誤りは計4件（`difficultyHp` の参照有無／進化階の計算／`dt` 上限の既存/ `drawProgress` という存在しない関数名）。いずれも修正済み
- **教訓として `DECISIONS.md` に「シンボル名は必ず検索で裏を取る」を規約として追加した**
- ZIP内に危険なパス・シンボリックリンク・認証情報・外部通信はなく、scout6のJavaScript構文も正常

## 次の作業

1. **第7A便を発射する。** `docs/orders/2026-08-08_7A_v2.md` と `game/underborn_scout6.html` をCodexへ渡す
2. 納品後、四段検品を回す
3. **Kevinが遺跡50階を通しで測る。** `?dev=1` のハート無限を使う。無傷通過の時間でよい
4. 測定結果を待って、支点の本数・ハート持ち越し・階数の妥当性を判断する
5. 7B・7Cの発注文を書く

## 次のAIへの注意

- **`REJECTED_IDEAS.md` を必ず読むこと。** 完成体、支点の耐久回数、地図の常時表示など、既に判断済みの案が多数ある
- 詩は正典。一字一句・改行位置とも変更禁止。発注文には毎回全文を貼る
- 7Aのスコープを越えないこと。支点・ハート持ち越し・地上演出は後続便
- `game/underborn_scout6.html` の内容は今は変更しない
- 発注文の書式は `docs/orders/` の既存4本に倣う

## 確認待ち

| 項目 | 内容 |
| --- | --- |
| 森のコアの呼称 | 「息吹」は設計側の提案。Kevin未裁可 |
| 支点に戻ったときのハート数 | 「刺した時点の数」は設計側の提案。Kevin未裁可 |
| 支点の本数 | 3〜5本。測定後に確定 |
| ハートの階またぎ持ち越し | 採用は決定済みだが、投入は1便遅らせる |
| 2周目の道筋 | 同じ5土地の再走か、土地の自由選択か。未設計 |
| 10月の評価提出物 | 何を出すか未定。評価用に階数を落とした分岐を作るか |
