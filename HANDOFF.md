# 引き継ぎ

## 現在の状況

- GitHub: `kj-keeley/under-born` / `main`
- 正典本体: `game/underborn_scout7bb.html`（第7B-b1便）
- 元の `game/underborn_scout7b.html` は未変更
- 7B-b1は実装・対象機能の検証完了。遺跡50階の人手による通しプレイは未実施

## 反映内容（2026-08-13）

1. 息止めの基準時間とCTを `P.breathMax` / `P.breathCd` へ移し、開発パネルの「操作」で調整可能にした
2. 息止め中は段階・コアにかかわらず、体・影・殻・装飾・金フラッシュ・衛星破片を描画せず、`mBlink(3.4, 0)` で瞬く目だけにした
3. 息止め中はマスター由来の `cut()` / `glow()` と範囲輪を描画しない。祭壇と敵の光は維持
4. 息止め解除時、体の描画スケールだけを約0.28秒わずかに膨らませる。`master.r` は変更しない
5. 光半径の初期値を `r0=0.5 / r1=2 / r2=3`、祭壇を `altarR=1.5 / altarLit=1.5` にした
6. 光スライダーの下限を下げ、操作時に `0 ≤ r0 ≤ r1 ≤ r2` を自動維持するようにした
7. 発注文を `docs/orders/2026-08-11_7B-b1_v3.md` に保存し、正典資料を更新した

## 確認・テスト結果

- JavaScript構文確認、`git diff --check`: 正常
- `P` の差分: 指定された `r0/r1/r2/altarR/altarLit`と新設 `breathMax/breathCd` だけ
- 敵側の `torch/torchLit/fov/search/ePatrol/eChase`、発見式、`P.sightR=7`: 差分なし
- 基準6秒の段階別値: 段階1=6秒／段階2=7.8秒／段階3=9秒／真の姿=9秒
- 段階1で6秒後に自動解除し、調整したCT 7秒が開始することを実時間確認
- ポーズ中は息止めタイマーが停止し、調整値6秒は保持
- 息止め中: 段階1と、装飾が最も多い真の姿＋霞コアで、体・装飾・残光が消えて目だけ残ることを画像確認。全段階・全コアは同じ `hold` 早期分岐を通る
- 解除後の光半径: 0.94 → 1.10 → … → 2.00。`P.r1=2` を超えない
- 半径の逆転操作: `r0=3` で `3/3/3`、`r2=1` で `1/1/1` へ整列し、更新停止なし
- 実ブラウザの警告・エラー: なし

## 実装側の感触

- 暖色の光は明確に小さくなったが、`P.sightR=7` の地形視界と冷色の祭壇火が残るため、遺跡序盤の祭壇探しは操作不能にはなっていない
- 目は暗闇の中で位置が分かるが、身体が残っているようには見えない小ささと明るさに収まっている

## 見送り

- 敵の数値・発見式、光のグラデーション、遮蔽判定、種火連鎖、敵AI、マップ生成は変更していない
- 7B-b（孵化、地上演出、土地詩、ミニマップ、森コア等）と7C（支点）は未着手
- 旧デバッグボタンの `4F` / `7F` 表記は現在の割合進化と一致しないが、指定範囲外のため変更していない

## 実質未使用の定義

- `finishOpeningPoem()`：定義のみ
- `awakenedCount`：定義のみ
- `SPARK_RADIUS`：互換用で未使用
- `drawCrossSection()`：`show(..., true)` の呼び出しがなく画面に出ない

`startFirstSpotlight()` は `finishPoem()` から呼ばれるため生きている。

## 次の確認

1. 光を小さくした結果、実プレイで祭壇探しが困難になっていないか
2. 目だけの明るさが、見えなさすぎ／目立ちすぎのどちらにも寄っていないか
3. 段階1に目を出すことでベイビーの卵らしさが失われていないか
4. 一般配布用ビルド時は `DEV_DEFAULT=false` へ戻す
