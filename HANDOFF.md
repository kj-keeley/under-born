# 引き継ぎ

## 現在の状況

- GitHub: `kj-keeley/under-born` / 既定ブランチ `main` / 公開
- 第7A-hotfix便を `game/underborn_scout7ah.html` として実装。scout6・scout7aは比較用に無変更で残した
- 18階の進化儀式→暗転→初進化詩の単独表示までブラウザ確認済み
- 遺跡1〜50階の人手による通しプレイと所要時間・特殊階の体感評価は未実施
- 第7B・7Cは未着手

## 今回行ったこと（2026-08-10）

1. `?dev=1` に加え、タイトル左上5回タップでlocalStorageへデバッグ有効フラグを保存・解除する導線を追加
2. デバッグパネルへ無敵、段階1/2/3/自動、敵ゼロを追加
3. 各土地の最初の特殊階を進行率25%の境界へ移し、同じ土地の2枚目との旧階間隔を維持。25%未満では採用しない防御判定も追加
4. タブと画面上の副題を削除
5. `titleExit` と後続の `opening` を再クリック・キー入力でスキップ可能にした
6. 発注文とコードの食い違いを照合。`startFirstSpotlight()` は `finishPoem()` から呼ばれる生きたコードで、`drawCrossSection()` は表示契機がない
7. 正典・決定・タスク・発注文アーカイブをscout7ahの状態へ更新
8. 要約版だった `docs/orders/2026-08-08_7A-hotfix_v1.md` を、Claudeから届いた発注文全文へ復元

## 確認・検証結果

- JavaScript構文確認・`git diff --check`: 正常
- 元ファイル `game/underborn_scout7a.html`: 未変更
- `P` のバランス値: 差分なし
- 通常URLからタイトル左上5回タップ→デバッグ出現→再読み込み後も維持→同操作で解除: 実ブラウザ確認済み
- 無敵: 検証用の捕縛経路を通してもハート3を維持
- 段階指定: 1階で段階3を選択でき、`masterStage()` が段階3を返す経路とボタン状態を確認。段階は姿・点火時間・息止め時間へ作用するが、光半径は既存どおり `master.idx` と `P.r0/r1/r2` が決めており、発注文の「光の範囲も段階3」という前提とは一致しない
- 敵ゼロ: 有効化時に2体→0体、2階・17階・20階への移動後も0体を維持
- 特殊階: 遺跡12階は通常、13階は縦長。配置表上も全特殊階が各土地の25%以降
- タイトル: 副題なし。連続2クリックで開幕演出を待たず次へ進む
- 17→18階: `nextFloor()` の実経路で段階1→2の進化を開始。7.1秒時点でも儀式継続、9.2秒完了後に暗転を挟み、詩「大きくなりましたね、小さな火。／活き活きと、その火で照らすのです。」が単独表示
- ブラウザのコンソール警告・エラー: なし

## 見送ったもの

- `P` のバランス値、総階数、特殊階の敵数4/5/6、特殊敵タイプの3階解禁は変更していない
- `drawCrossSection()` の表示契機、スポットライト、7B、7Cは変更していない
- 遺跡1〜17階を祭壇攻略込みで人手通しはしていない。17→18階の遷移と詩を独立検証した
- `visibilitychange` の実機ロック／タブ切替は未確認。リスナーは7Aのまま保持

## 発注文とコードの食い違い

- `startFirstSpotlight()` は死んだコードではない。`finishPoem()` から呼ばれ、初回開始詩の後に実行される
- 段階1/2/3は `masterStage()` を通じて姿・点火時間・息止め時間を変えるが、通常時の光半径は段階では変わらない。この便では `P` と光のコア設計を変更しない指定を優先した
- `drawCrossSection()` は関数内から参照されるが、`show(..., true)` の呼び出しがないため画面には出ない

## 定義があるが実質未使用のもの

- `finishOpeningPoem()`：定義のみで参照なし
- `awakenedCount`：定義のみで参照なし
- `SPARK_RADIUS`：コメントどおり互換性目的で未使用
- `drawCrossSection()`：`show(..., true)` がないため画面表示されない

`startFirstSpotlight()` は `finishPoem()` から呼ばれるため、この一覧には含めない。

## 次の作業・確認質問

1. Kevinが無敵・敵ゼロを必要に応じて使い、遺跡50階を通して所要時間を測る
2. 特殊階が25%以降なら妥当か、最初の遺跡縦長13階の敵4体がまだ多すぎるかを体感で判断する
3. 断面図をどの画面で表示するか7Bで裁可する
4. 測定結果をもとに階数・支点本数・ハート持ち越しを判断し、7B・7Cへ進む
