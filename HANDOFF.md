# 引き継ぎ

## 現在の状況

- GitHub: `kj-keeley/under-born` / `main`
- 正典本体: `game/underborn_scout7bc.html`（第7B-b2便）
- 元の `game/underborn_scout7bb.html` は未変更
- 7B-b2は対象機能の実装・検証完了。息止め中の移動比率はKevinの実プレイによる確定待ち

## 反映内容（2026-08-14）

1. `P.breathMoveRate` を0〜1、刻み0.05、既定値1で追加した
2. 開発パネル「操作」に「息止め中 移動比率」を追加した
3. 比率0では通常の床方向への1マス移動を座標更新前に拒否し、向きの変更だけを許可した
4. 霞の壁抜けは停止判定より前の壁分岐、転位と暗火は通常移動と別経路のまま維持した
5. 比率0より大きい場合、1マス開始時の実効秒数を `P.mSpeed / P.breathMoveRate` として `master.spt` に固定した
6. `mSpeed` スライダーによる移動途中の `master.spt` 直接変更を削除し、新しい値は次のマスから反映するよう統一した
7. 発注文を `docs/orders/2026-08-11_7B-b2_v3.md` に保存し、正典資料を更新した

## 確認・テスト結果

- JavaScript構文確認、`git diff --check`: 正常
- 比率1: 通常移動を受け付け、`master.spt=P.mSpeed`になる
- 比率0: 通常の床移動入力で`master.tx/ty`が変わらず、向きだけ変わる
- 比率0.5: `master.spt=0.32`となり、通常0.16秒/マスの半速になる
- 移動途中に息を吐いても、そのマスの`master.spt`は変わらない
- 移動途中に`mSpeed`スライダーを0.16→0.4へ変更しても、そのマスの`master.spt=0.32`は変わらない
- 比率0で霞の1マス壁抜け、転位の位置交換、暗火の消灯点火可否と残り時間半減が動作する
- 実ブラウザの警告・エラー: なし
- 敵・光・発見式・既存`P`既定値は変更していない

## 実装側の感触

- 完全停止0は、追われてから息を止めても逃げられないため、「先に隠れて待つ」方向へ明確に寄る
- 一方で、曲がり角で侵入者と鉢合わせた場合は反応手段が固有能力に限られ、初期コアではかなり厳しい。理不尽かどうかはKevinの実プレイ判断が必要
- 暗火は消灯点火そのものは保たれるが、停止中に祭壇まで位置調整できないため、0を採用する場合は運用感の確認が特に必要

## 見送り

- `breathMoveRate`の既定値は1のまま。0や中間値への焼き込みは行っていない
- 潜伏半径・全開半径・縮む秒数の内部値化、祭壇2項目の改名は7B-b1検品後の別便
- 敵の数値・発見式、光の描き方、種火連鎖、敵AI、マップ生成は変更していない
- 7B-b（孵化、地上演出、土地詩、ミニマップ、森コア等）と7C（支点）は未着手

## 実質未使用・到達不能の定義

- `finishOpeningPoem()`：定義のみ
- `awakenedCount`：定義のみ
- `SPARK_RADIUS`：互換用で未使用
- `drawCrossSection()`：`show(..., true)` の呼び出しがなく画面に出ない
- `master.idx>=2` の分岐：`master.idx`を2へ設定する処理がなく到達不能

`startFirstSpotlight()` は `finishPoem()` から呼ばれるため生きている。

## 次の確認

1. 完全停止0が、特に曲がり角の鉢合わせで理不尽に感じるか
2. 0と1の中間（例0.6）に、隠れる行為と操作余地が両立する値があるか
3. 完全停止時に暗火コアの運用が壊れていないか
4. 採用値が決まったら、別便で`P.breathMoveRate`の既定値へ焼き込む
