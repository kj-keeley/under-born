# 引き継ぎ

## 現在の状況

- GitHub: `kj-keeley/under-born` / `main`
- 正典本体: `game/underborn_scout7b.html`（第7B-a便）
- 元の `game/underborn_scout7ah.html` は未変更
- 7B-aは実装・主検証完了。遺跡50階の人手による通しプレイは未実施

## 反映内容（2026-08-12）

1. デバッグパネルと開閉ボタンをシネマティック中も使用可能にした
2. 無敵・段階・敵ゼロ・階ジャンプを先頭に移し、調整値を初期全閉のアコーディオンにした
3. パネルを画面内の独立スクロール領域にした
4. プレイ中に現在階／総階数を表示した
5. セーブしないポーズを追加。敵・配下・タイマー・入力を停止し、息止め解除は再開時まで保留する
6. `DEV_DEFAULT=true`、`?dev=0/1`、localStorage `"0"/"1"` の三状態判定と常時 `DEV` 表示を追加した
7. 発注文を `docs/orders/2026-08-11_7B-a_v3.md` に保存し、正典資料を更新した

## 確認・テスト結果

- JavaScript構文確認、`git diff --check`: 正常
- URLなし: DEV既定ON。タイトル中もパネル開閉可
- `?dev=0`: `DEV`表示・「数値」・パネルは非生成
- 隠し5タップ: ON→保存OFF→再読込後もOFF→再度5タップでONを確認
- パネル: 初期スライダー0本、1グループのみ開く、閉じて再開で全閉、背後画面はスクロールしない
- 階数: 遺跡 `18 / 50`、沼 `5 / 38` を確認
- ポーズ: 停止中はタイマー不変。息止め中にポーズ→離す→再開で即解除し、その時点からCT開始
- 中断してタイトルへ: 階・ハート・祭壇・配下の一時状態を破棄
- 主検証: タイトル→遺跡17階→18階進化儀式→暗転→初進化詩→チャイルド表示のポーズ→再開を実ブラウザで確認
- ブラウザコンソールの警告・エラー: なし

## 見送り

- `P` の初期値、光と息止め、種火連鎖、敵AI、マップ生成、`renderMasterBody` は変更していない
- 7B-b（孵化、地上演出、土地詩、ミニマップ、森コア等）と7C（支点）は未着手
- 旧デバッグボタンの `4F` / `7F` 表記は現在の割合進化と一致しないが、指定範囲外のため変更していない

## 実質未使用の定義

- `finishOpeningPoem()`：定義のみ
- `awakenedCount`：定義のみ
- `SPARK_RADIUS`：互換用で未使用
- `drawCrossSection()`：`show(..., true)` の呼び出しがなく画面に出ない

`startFirstSpotlight()` は `finishPoem()` から呼ばれるため生きている。

## 次の確認

1. 進行度表示が画面の静けさを壊していないか
2. `DEV` の位置と大きさが適切か
3. ポーズボタン追加で息止めが押しにくくなっていないか
4. アコーディオンの初期全閉が逆に手数を増やしていないか
5. 一般配布用ビルド時は `DEV_DEFAULT=false` へ戻す
