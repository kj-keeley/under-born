# 引き継ぎ

> **2026-09-03 設定（Claude）**：中断解除。便は Kevin 待ち無しで連続、判断は「暫定」行で進める、Kevin は最終決定4つのみ。詳細は `DECISIONS.md` 運用・体制 2026-09-03 と `TASKS.md`。2026-09-05 に Codex が内容を確認し、正典へ反映。


## 現在の状況

- GitHub: `kj-keeley/under-born` / `main`
- 正典本体: `game/underborn_scout7g.html`（穴とハーケンの絵便）
- 元の `game/underborn_scout7f.html` 以下は未変更
- 穴とハーケンの絵便は `game/underborn_scout7g.html` への実装と便単位のブラウザ確認を完了。通常階の全灯後は安全状態へ戻し、光の穴へ自分で入って既存昇階へ進む。縄つきハーケンは授与・HUD・床で共通表示する
- 次は一般配布用の `DEV_DEFAULT=false` 戻し。遺跡50階の通しプレイとハーケン本数を含む手触り確認も残る

## 反映内容（2026-09-08・穴とハーケンの絵便）

1. `game/underborn_scout7f.html` を保存したまま `game/underborn_scout7g.html` を作成した
2. 通常階の全灯判定を1回だけにし、0.6秒の解放脈動後は同じ階の操作へ戻す。最後に灯した祭壇の隣へ光の穴を出し、自分で入った時だけ既存の `rise → floorCard → open` へ進むようにした
3. 穴が開いた後の解放住民を、追跡・接触・消灯・配下との交戦・経路更新から外した。ミニマップは暖色の点だけにして視野錐を消した。進化階と土地最終階は従来経路を維持した
4. C案の縄つきハーケンを共通描画にし、女神の授与、通常・暗闇HUD、打った床へ反映した。HUDは固定値でなく実際の残数を表示する
5. 「これを、あなたに。ハーケンです。」で大きな絵を出し、クリック後0.5秒でHUDへ飛ばしてから本数と次の行を表示する取得フェーズを追加した。飛行中のクリック・Enterは受け付けない
6. 開発パネルへ、保存・解放数・クリア登録を変えず見た目だけ確認する「穴を出す」を追加した。`DEV_DEFAULT=true` は変更していない

## 確認（2026-09-08・穴とハーケンの絵便）

- Node で単一HTML内スクリプトの構文、正典詩・`isWall`・`mChip` の不変、通常階の穴待ちと進化階・最終階の分岐、解放住民の除外、ハーケン3表示先と実数参照を静的確認。`git diff --check` 通過
- 実ブラウザで「穴を出す」後に「光の穴が開いた」と表示され、1階のまま操作へ戻って待ち続けても自動昇階しないことを確認した
- 実ブラウザで、女神の2行目開始時に大きな縄つきハーケンが現れ、クリック後の飛翔中に追加クリックしても次行を飛ばさず、着地後に絵＋実数 `×5` と次行が現れることを確認した
- 授与会話の残りを終えた後、通常HUDにも同じ絵＋`×5` が表示されることを確認した。開発導線の穴へタイル移動で入る再現は操作が安定しなかったため、`master.t>=1`・座標一致から `startAscent('rise')` へ接続する経路を静的確認で補った

## 見送り（2026-09-08・穴とハーケンの絵便）

- 穴に吸われる絵、住民の絵、ハーケン以外のHUD、バランス数値、`DEV_DEFAULT=false`、長い通しプレイは変更・実施していない

## 反映内容（2026-09-08・点火無料便）

1. `game/underborn_scout7e.html` を保存したまま `game/underborn_scout7f.html` を作成した
2. プレイヤーが祭壇を灯す経路から、`hearts--`、`♥-1` 表示、支払い粒、`afterHeartLoss()` の4処理だけを外した
3. 祭壇の所有変更、配下生成、点火音と演出、コアの点火コスト、息吹の息止め解除、ロード／真の姿の増援、種火伝播は維持した
4. 住民接触の `♥-1`、種火連鎖の `♥+1`、上限7、昇階時の最低3、無敵モード、既存の詩・女神・ハーケン・昇階・住民処理は変更していない

## 確認（2026-09-08・点火無料便）

- Node で単一HTML内スクリプトの構文を確認し、7e→7f の本体差分が自己点火の4行削除だけであること、正典詩が不変であることを確認。`git diff --check` 通過
- 実ブラウザで、3♥から自己点火した直後に3♥・自分の火1/6のまま、続く種火連鎖で6♥・自分の火4/6になることを確認した。自動連鎖が先に進むため、同一盤面で手動点火を3回続ける形にはならなかった
- 階をやり直して住民接触により3→2♥・捕まった1となることを確認した
- その場で無敵をONにし、接触圏内で待っても2♥・捕まった1のまま変化しないことを確認した

## 見送り（2026-09-08・点火無料便）

- 難易度の再調整、ハートの呼称変更、後続便、`DEV_DEFAULT=false`、長い通しプレイは行っていない

## 反映内容（2026-09-07・昇階・住民便）

1. 画面上の「侵入者」を「住民」へ改め、I / L / T / R / B / H 六種全てに名前と初登場の一言を追加した。既存の3.8秒ズームの下部で文字送りし、既読は六種すべて保存する
2. 祭壇を全灯した時に、その階の住民を停止・ベイビー向き・暖色の光輪・浮遊・音符で解放し、`activeRun.freedByFloor` へ階別スナップショットを上書きするようにした。ハーケン保存と最終階で1回だけの通算加算へ接続した
3. 通常階に、解放脈動→光の穴へ浮上→次階・断面図カード→新階が開く、の2.4秒昇階を追加した。進化階は従来の儀式のみとし、10 / 20 / 30 / 40階への到着だけカードを3秒に延ばして土地分の住民を並べた
4. 地上は土地の詩の後（遺跡は地上生成後）にその土地の住民を並べ、森は女神と重ねず変身後に通算住民を3秒表示してからエンディングへ進むようにした
5. 開発パネルに「住民の列を試す」を追加した。`DEV_DEFAULT=true`、正典詩、進化階、敵AIと形、バランス値は変更していない

## 確認（2026-09-07・昇階・住民便）

- Node で単一HTML内スクリプトの構文、六種の `ENEMY_INFO`、初登場台詞の字数、保存の正規化、正典詩不変を静的確認。`git diff --check` 通過
- 実ブラウザで、解放住民に暖色の光が灯り、ベイビーの方を向いて浮遊し、光輪と音符を出すことを確認した
- 実ブラウザで、「遺跡 10F」、到着階を示す断面図、「ここまで灯した住民」と六種の住民列、スキップ後の10F操作再開を確認した
- 新規開始で初見住民の3.8秒ズームへ入ることを確認。下窓の表示期間と文字送りはコード経路と定数で照合した
- 実ブラウザの警告・エラー: なし。六種のうち `ENEMY_INFO` が無い特殊住民: なし

## 実装側の感触（2026-09-07・昇階・住民便）

- 解放直後に冷色の身体は保ったまま暖色の光と輪が灯り、音符も出るため、「倒した」より「戻った」と読みやすい
- 10Fカードは、階数と断面図の下に住民列が収まり、プレイの節目と累積の両方が一画面で読める

## 見送り（2026-09-07・昇階・住民便）

- 住民の絵の描き直し、正典詩、進化階の儀式、敵AI、土地構成、ハーケン、バランス数値、`DEV_DEFAULT=false` は変更していない
- 住民名・初登場台詞と昇階演出の長さは暫定。最終稿と通しプレイでの手触り確認が残る

## 反映内容（2026-09-07・ハーケン便）

1. 画面上の「支点」を「ハーケン」へ改名し、現行仕様と台帳にも旧称を残して反映した
2. 4秒長押しの1・2・3秒に金属音と専用火花、成功時に完了音・小さな揺れ・杭の点火・ベイビーの光の脈動・帯表示、途中中断時に失敗音と帯表示を追加した
3. ハート0後を `dim → candle → text → return` に分け、キャンドルの炎と煙、「火が絶えた」、ハーケンの火から盤面が戻る復帰演出を追加した。演出終了まで入力は遮断する
4. 初回だけ女神がハーケンを渡す4行を追加し、授与までボタン・Eキー・HUD本数を隠した。初回復帰は消費せず `⚑×5`、2回目以降は復帰時に1本消費する
5. 開発パネルに「渡す場面をもう一度」を追加した。`DEV_DEFAULT=true` と既存の正典詩・バランス値は変更していない

## 確認（2026-09-07・ハーケン便）

- Node で単一HTML内スクリプトの構文と、所定の音・状態・表示文字列を静的確認。`git diff --check` 通過
- 実ブラウザで、新規開始時はハーケンボタンと `⚑×N` が出ず、初回ハート0でキャンドル消灯・煙・女神・Canvas右上の `⚑×5` を経て操作可能な盤面へ戻ることを確認した
- 初回復帰後にハーケンボタンとHUD `⚑×5` が現れ、2回目は女神を挟まず約6秒で戻って `⚑×4` になることを確認した
- ハーケン長押し中断で「ハーケンが落ちた」の帯を確認した。打ち切り側はコード経路（1秒ごとの3打、4秒で `completePinHold`、成功音・火花・揺れ・点火・帯）を確認した

## 実装側の感触（2026-09-07・ハーケン便）

- キャンドルの炎が消えたあとに煙だけが残るため、従来の短い暗転より「火が絶えた」区切りが読みやすい
- 初回だけ授与を挟み、その後は同じ復帰演出を短く通すため、説明の反復感はない

## Claude 検品（2026-09-07・第7C便）

- 差分：正典の詩は不変。レビュー9点はすべて入っている——消費は復帰時のみ（`afterHeartLoss` 内の `pinsLeft--` だけ。`pinAtMaster` は減らさない）／復帰直後に `syncRunCheckpoint()` で残数保存／接触（`caught`）と点火（L1113）の両方から `afterHeartLoss()` をクリア判定より前に呼ぶ／敵接触で `cancelPinHold()`（L1336・1464）／設置開始は `master.t>=1` かつ息止め中でない（`beginPinHold`）／「つづきから」は `resumeRun`→`restoreCheckpoint` で `startTheme` を通らない／「支点を消す」は保存と `pin` の両方。
- 発注文どおり。push して正典に。これで遺跡 MVP の部品（詩・進化・支点）が揃った。次は50階の通しプレイ（所要時間）＝Astra の出番。

## 反映内容（2026-09-06・第7C便）

1. 土地開始時に消費なしの0本目を自動設置し、Eキー／「支点」ボタンの4秒長押しで直近の支点を置き換えるようにした
2. 設置中は新しい移動入力を止め、キー・ボタンを離す、敵へ接触する、息止めを始める、の各条件で進捗を0へ戻すようにした
3. 接触と祭壇点火のハート減少を `afterHeartLoss()` に集約し、ハート0で0.8秒暗転後に支点へ復帰、本数を1減らして即保存するようにした
4. 支点なし／残り0本では土地1階へ戻し、ハート3・本数初期値・0本目の支点を作り直すようにした
5. `underborn_run_v1` を追加し、支点設置・0本目設置・復帰後の残数更新だけを保存。タイトル開始後に「つづきから／はじめから」を出すようにした
6. HUDの `⚑×残数`、盤面とミニマップの支点印、開発パネルの本数3〜5・「支点へ戻るを試す」・「支点を消す」を追加した

## 確認・テスト結果（第7C便）

- 埋め込み JavaScript の構文確認、`git diff --check`: 正常
- 実ブラウザで新規土地の0本目と `⚑×5`、画面の「支点」ボタン、盤面上の杭と残り火を確認した
- ポーズからタイトルへ戻り、「つづきから（遺跡 1階・支点あり）」を経て女神を再生せず盤面へ戻ることを確認した
- 開発パネルの支点本数3〜5、復帰試験、支点消去の各UIを確認した
- 開発パネルからハート0にし、0.8秒の「火が絶えた」暗転後に1階の支点へ戻り、HUDが `⚑×5` から `⚑×4` へ減ることを確認した
- リロード後も「つづきから」が出て、復帰後の残数 `⚑×4` が保持されることを確認した
- 実ブラウザのページエラー: 0件

## 実装側の感触（第7C便）

- 支点の杭と小さな火は暗闇の上でも読めるが、プレイを邪魔するほど大きくない
- ハート0から盤面へ戻るまで0.8秒なので、失敗は認識できつつ再開待ちは短い。長い通しプレイで「軽すぎる」かは要確認

## 見送り（第7C便）

- 支点本数5の確定、階数再調整、ハートの階またぎ仕様、`DEV_DEFAULT=false`、エンディング詩・ロゴは変更していない
- 4秒設置の押し切り、3秒離し、敵接触による中断、点火での最終ハート消費、本数0からの1階復帰はコード経路を照合した。実プレイでの一連操作は次の通し検品にも残す

## Claude 検品（2026-09-06・第7B-b便）

- 差分：正典の詩7本（ask 含む）は不変、追加は `land` 4本のみ。クリア記録は通常クリア経路（L1357）で1回、`startSurfaceEnding(registerDungeonClear(curTheme))` の形。デバッグ経路は登録なし。ミニマップは `breathMinimapAlpha` で息止め中のみ、`mapBtn2` は消えている。敵紹介の既読は `campaign.seenEnemyIntros` に保存・復元、`startTheme` の clear は無し。真の姿のタイマーは `chainPause<=0` のときだけ進む。教えは同じ敵で距離と `los` を見て、息止め中は出ない。配下上限は `normalMinionCap()`（息吹で2倍）で生成と表示が共通。息吹は `CORES.breath`、`CORE_REWARDS.forest='breath'`。孵化は `ubTitleEgg`。レビュー用 Sol の9点はすべて反映されている。
- 発注文どおり。push して正典に。

## 反映内容（2026-09-06・第7B-b便）

1. 最終階クリア時に進行を1回だけ保存し、その結果を地上演出へ渡した。デバッグ地上演出は保存しない
2. 5土地の地上色を分け、砂漠・火山・沼・森の土地詩を女神なしの下窓へ追加。森は土地詩→女神→初変身の順にした
3. タイトルを亀裂なし・目なしの卵にし、開幕5.2秒内へ亀裂3本、殻の破片、正典ベイビー、暗転を入れた
4. 地図ボタンを削除し、息止め中だけ0.15秒フェードでミニマップを表示した
5. 森クリアで覚醒する「息吹」を追加。息止め中点火、点火時の息止め解除、通常配下上限2倍とHUD表示、葉2枚の姿を実装した
6. 特殊敵紹介の既読保存、進化・敵紹介・教えのシネマティック化、連鎖停止中の真の姿タイマー停止、教えのLOS条件と完了時保存、進行リセット初期化を反映した

## 確認・テスト結果（第7B-b便）

- 埋め込み JavaScript の構文確認、`git diff --check`: 正常
- 実ブラウザで、タイトルの卵→孵化後のベイビー→女神 `dark` への接続を確認した
- 5土地の空・地面の色差と同一形状、遺跡のみ詩なし、砂漠・火山・沼・森の正典詩、森の「土地詩→女神」を確認した
- デバッグ地上演出を5土地で実行して `campaign.clears` が空のまま、通常の森クリア登録は1回、息吹覚醒、「森のコアが目覚めた」表示を確認した
- 地図ボタンが存在しないこと、息止めミニマップ、祭壇2基で通常上限2・息吹上限4、息吹点火直後に息止め終了・CT5秒を確認した
- 連鎖停止中に真の姿の残り時間10秒とCT10秒がともに変わらないこと、敵紹介 `R` の既読が保存され土地開始後も残ることを確認した
- 実ブラウザのページエラー: 0件

## 実装側の感触（第7B-b便）

- 明るい砂漠と赤黒い火山は、同じ地上の形でも土地の違いが一目で分かる
- 息止めで盤面が暗くなる代わりにミニマップが開くため、地図ボタンなしでも次の一手を考えられる

## 見送り（第7B-b便）

- 7C（支点、ハートの階またぎ）、暫定4件の数値、到達不能コード整理、`DEV_DEFAULT=false`、エンディング詩・ロゴは変更していない
- 「息吹」の呼称と女神 v2 の追加台詞は最終裁可待ち

## Claude 検品（2026-09-06・女神便 v2）

- 差分：正典の詩6本は一字も変わっていない（opening/child/lord/release/trueForm/breathLesson を bd と突き合わせ）。追加は `ask`／`refuse`／`callback` のみ。息止めの教えは無変更。`campaign.refusals` を保存・復元している。
- 実ブラウザ（Claude 側、描画ループを手で進めて確認）：真っ暗＋文字送り「小さき火よ。目を覚ますのです。」▼ → クリック → まぶたが開いて暗闇に女神だけ（足元に光、ベイビーは火の点）→ 「火をともしてくれますか？」＋ はい／いいえ の小窓 → いいえ×3 で「ほんとうに、ことわるのですか？」「え。……ほんとうに、ですか？」「……まってください。／ごほん。…／どうか、小さき火よ。…／火を、ともしてくれませんか？」と変わる → はい → 「さぁ、起き上がって。上へ上へいってらっしゃい。」→ 女神が光に溶けながら盤面が見えてくる → 遊び。`campaign.refusals=3` が保存された。
- 発注文どおり。窓は女神にかぶらない。残るのは Kevin の目（台詞の最終稿、まぶたの速さ、光の広がり方）。
- **未コミット**（`git status`：`game/underborn_scout7bg.html` untracked、台帳 modified）。Sol にコミットさせること。

## 反映内容（2026-09-06・女神便 v2）

1. 女神の全場面を「声が先、姿はあと」に組み直し、吹き出しを画面下の文字送り窓へ置き換えた
2. 開始を、暗闇の1行目→1.2秒のまぶた→女神とベイビーの火だけ→問いと選択→2行目→2秒の光による盤面開示、の順にした
3. 「はい／いいえ」を追加し、拒否3段と4回目以降の反復、拒否回数の保存、3回以上での初進化・森の追加台詞を実装した
4. 初進化・2回目の進化を、儀式後の暗闇の声→女神だけ→残りの行→光で新階が見える順にした。儀式スキップ時の女神なしは維持した
5. 森を、白い光の中の解放詩1行目→地上と女神→残りの台詞→女神退場→既存時間の初変身へ接続した

## 確認・テスト結果（女神便 v2）

- 埋め込み JavaScript の構文確認、`git diff --check`: 正常
- 新規開始で、暗闇の文字送り→まぶた→女神だけ→問いと選択→2行目→女神が光へ溶けて盤面が見える→操作可能、を実ブラウザ確認した
- 「いいえ」で言い方が変わり、3回目は4行、4回目以降は「……どうか。」になり、「はい」でのみ先へ進むことを確認した
- 拒否4回後の「はい」を保存し、初進化で追加行、森で「四度も」と正確な漢数字および「……ありがとう。」が出ることを確認した
- 進行リセット後（拒否0回）の初進化では、正典2行目の次に回想を挟まず光の場面へ進むことを確認した
- 「4Fの詩を試す」で儀式と文字が重ならず、暗転後に1行目→女神→2行目→追加行へ進むことを確認した
- 森の地上演出で、白い光→女神と地上→解放詩2行目→拒否回想→変身前詩→女神退場→初変身を確認した
- 実ブラウザの警告・エラー: なし

## 実装側の感触（女神便 v2）

- 声、姿、問い、返事、光の順が分かれたため、女神が突然ゲーム盤へ湧く印象はなくなった
- 下窓は最長行でも実寸で収まり、女神やベイビーを隠さず、各場面で同じ読み方ができる

## 見送り（女神便 v2）

- 女神の絵・大きさ、既存の正典詩、進化儀式、敵紹介、息止めの教え、敵AI、光・種火連鎖、マップ生成は変更していない
- 問い・拒否・回想の追加台詞は暫定のまま。第7B-b便、第7C便は未着手

## 反映内容（2026-09-06・女神便 hotfix）

1. 女神の矩形を基準に、吹き出しを頭上（4ドットの隙間）→左右→画面上端の順で置き、尻尾は顔を横切らず口元側へ向けた
2. 登場を「光だけ0.5秒→女神が0.8秒で現れる→会話」、退場を「吹き出し消去→女神が0.9秒で光へ溶ける」に分けた
3. 正典詩を一行ずつ表示し、クリック／Enter／スペースまたは3.6秒で次行へ進めるようにした。続きがある行だけ点滅する「▼」を出す
4. 森の解放詩は既存の表示時間を二分し、二行を自動で一行ずつ表示するようにした

## 確認・テスト結果（女神便 hotfix）

- 埋め込み JavaScript の構文確認、`git diff --check`: 正常
- 新規開始で、光→女神（吹き出しなし）→吹き出し、の順を実ブラウザ確認した
- 開発パネル「女神を試す」のランダム配置10例で、吹き出し本体・尻尾とも女神の顔や身体へ重ならないことを確認した
- クリックと Enter で一行目→二行目→吹き出し消去→女神退場→遊び再開を確認した。会話前のクリックは進行させない
- 3.6秒ごとの自動送りで一行目→二行目→退場→遊び再開を確認した
- 森の地上演出で解放詩二行が前半・後半に一行ずつ自動表示され、続いて変身前詩へ進むことを確認した
- 実ブラウザの警告・エラー: なし

## 実装側の感触（女神便 hotfix）

- 吹き出しと女神の間に盤面が見える隙間ができ、顔が隠れず人物として読みやすくなった
- 光だけの予告と、女神だけを見せる一拍が入ったため、台詞が誰のものか追いやすい

## 見送り（女神便 hotfix）

- 女神のサイズ・デザイン、正典詩、進化儀式、息止めの教え、敵AI、地上演出の既存秒数は変更していない
- 第7B-b便、第7C便は未着手

## Claude 検品（2026-09-06・女神便）

- 差分を読んだ：`CANON_POEMS` は一字も変わっていない。`maybeStartBreathLesson`〜`drawBreathLesson` は無変更。進化は `updateEvolutionBlackout` で `beginPendingFloor()` → `beginGoddess(lines)` の順（儀式と重ならない）。森だけ `story=forest`。女神の1ドット＝`TS/27`（高さ1マス）。吹き出しは名札なし・`CS*.025`・改行は配列のまま。発注文どおり。
- 実ブラウザ（Claude 側、`python -m http.server` で `scout7be` を開いた）：新規開始→スポット→女神の順で動いた。開発パネル「女神を試す」で、ベイビーの左下1マスに女神、頭上に黒い吹き出しで開始詩2行、しっぽは口。読める。
- 注意：Claude 側のブラウザは画面が隠れると描画ループが止まるため、吹き出しの表示は `goddessScene.phase='talk'` を手で進めて確認した。実機の流れは Sol の確認どおり。
- 残るのは Kevin の目：「小」で女神に見えるか（Sol も同じ所を挙げている）。却下が無ければ採用。

## 反映内容（2026-09-06・女神便）

1. 高さ約1マスの人型ドット絵 `renderGoddess` と、正典の改行を保持する名札なしの黒い吹き出し `drawSpeechBubble` を追加した
2. 遊びを止める `mode='goddess'` を追加し、0.5秒で現れる→詩を表示→0.5秒で消える→無敵1.2秒で復帰する流れにした
3. 新規開始を、初回スポットライトの後に女神が開始詩を喋る順へ変更した。2回目以降はスポットなしで女神が出る
4. 進化後は儀式→0.6秒暗転→新しい階を生成→女神の順へ変更した。儀式スキップ時は従来どおり詩ごと省く
5. 森の地上だけ `story=true` とし、解放詩と変身前詩を地上の女神＋吹き出しへ置き換えた
6. 開発パネルに「女神を試す」を追加し、Enter／スペース／タップで台詞を飛ばせるようにした

## 確認・テスト結果（女神便）

- 埋め込み JavaScript の構文確認、`git diff --check`: 正常
- 新規オリジンの実ブラウザで、開始から5.6秒時点にスポット、10.3秒時点に盤面上の女神と開始詩が出る順を確認した
- 「4Fの詩を試す」で儀式画面を確認後、10.5秒時点に新しい盤面上で初進化詩を喋る女神を確認した。儀式と女神は重ならない
- 森の「地上演出を試す」で、解放詩と変身前詩が女神の吹き出しに出ること、変身時に吹き出しが残らないことを確認した
- 「女神を試す」から Enter で消して遊びへ戻れることを確認した
- 実ブラウザ2オリジンの警告・エラー: なし

## 実装側の感触（女神便）

- 女神は実寸でも白いローブ・水色の髪・光輪が一体の人物シルエットとして読める。閉じた目など顔の細部は小さいため、最終的な「女神に見えるか」は Kevin の確認が必要
- 吹き出しは遊び画面上でも十分読め、最長の解放詩も左右にはみ出さなかった
- 出現位置は確認した開始・進化の両盤面で床上に収まり、壁へ埋まらなかった。候補は `freeNear` の通行可能判定を使う
- 森では女神を左、ベイビーを中央に分けたため、確認したフレームでは変身の見せ場を遮らなかった

## 見送り（女神便）

- 息止めの教え、敵紹介、進化儀式自体、敵AI、光、種火連鎖、マップ生成、地上演出の既存秒数は変更していない
- 7B-b、7C、到達不能コードの掃除は未着手
- 進化のたびの登場頻度がくどくないかは、複数土地をまたぐ長いプレイで確認する

## 反映内容（2026-08-16）

1. 敵の追跡対象を `chaseTarget='master'/'minion'` で分離した
2. マスターを見失った敵は最後の位置から1〜2マス手前の到達可能地点まで追い、2マス以内で捜索へ入るようにした
3. `lookReason` を導入し、マスター喪失時だけ「？」を出して周辺2〜3マスを歩き、時間切れ後に巡回路へ戻すようにした
4. 息止め中かつ未発見なら、敵の経路探索と次の一歩からマスターの現在マスを除外した。開始済みの一歩は完了させる
5. 真の姿で燃えた敵は `P.search` と切り離し、炎マークのまま3秒間、経路生成も移動も行わないようにした
6. 既存の捜索スライダーを「見失って探す秒数」、0.5〜15秒、刻み0.5へ変更した。既定値2は維持した
7. 発注文を `docs/orders/2026-08-11_7B-b3_v4.md` に保存し、正典資料を更新した

## 確認・テスト結果

- JavaScript構文確認、`git diff --check`: 正常
- 距離8マスでマスターを見失った敵が、その場で止まらず接近し、距離2マスで `lostMaster` の捜索へ入る
- 捜索開始時に「？」を出し、周辺を歩いた後、`_returning` を伴って巡回路へ戻る
- 息止め後に残った経路の次ノードがマスターのマスなら破棄され、別経路へ進む。マスターの `tx/ty` へ新規進入しない
- 配下追跡は従来どおり1.5秒後に `lostMinion` へ移る
- 炎上中は敵の `tx/ty` と `fx/fy` が3秒間変わらず、`P.search` に依存しない
- `P.search=10`、段階1、開けた通路、再発見可能距離の固定条件では、3秒の息止めが先に切れ、その後 `chaseTarget='master'` で再発見する
- 開発パネルの捜索スライダーはラベル、最小0.5、最大15、刻み0.5、値2を実ブラウザで確認した
- 実ブラウザの警告・エラー: なし
- `P.breathMoveRate`、`P.search`の既定値、`seesMaster` / `inArc` / `los`、配下追跡の既存タイマーは変更していない

## 実装側の感触

- 既定2秒では、敵が手前まで来て「？」を出すため、息止め直後に追跡が消える従来より圧は明確に増えた
- 2秒の探索が息止め3秒より短いため、開けた場所では巡回復帰後も再発見の余地がある一方、遮蔽の多い場所では逃げ切りやすい。簡単すぎるかはKevinの実プレイ判断が必要
- 手前2マス停止は通常通路では自然だった。幅1通路、行き止まり、複数敵の固まり方は追加の実プレイ確認が必要
- 炎上3秒停止は真の姿を確実に強くするため、強すぎないかは2周目の実プレイで確認が必要

## 見送り

- `breathMoveRate`の既定値は1、`P.search`の既定値は2のまま。数値の焼き込みは行っていない
- `seesMaster` / `inArc` / `los`、敵の視界・速度・人数、配下追跡、マップ生成は変更していない
- 潜伏半径・全開半径・縮む秒数などのデバッグ項目整理は未着手
- 7B-b（孵化、地上演出、土地詩、ミニマップ、森コア等）と7C（支点）は未着手

## 実質未使用・到達不能の定義

- `finishOpeningPoem()`：定義のみ
- `awakenedCount`：定義のみ
- `SPARK_RADIUS`：互換用で未使用
- `drawCrossSection()`：`show(..., true)` の呼び出しがなく画面に出ない
- `master.idx>=2` の分岐：`master.idx`を2へ設定する処理がなく到達不能

`startFirstSpotlight()` は `finishPoem()` から呼ばれるため生きている。

## 次の確認

1. `P.search=2`で、息止めだけで確実に逃げ切れてしまわないか
2. 手前2マス停止が狭い通路や行き止まりで不自然にならないか
3. 複数敵が同じ最後の位置へ集まりすぎないか
4. 到達可能な手前地点がなく、即捜索へ入る地形がどの程度あるか
5. 敵の松明内で息を止めても発見中のままになる挙動が理不尽でないか
6. 炎上3秒停止で真の姿が強くなりすぎていないか
7. 息止め中の移動比率0〜1の採用値を決める
